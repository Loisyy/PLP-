ead & Write Challenge with Error Handling

def modify_content(line):
    """
    Example modification function.
    You can customize this function to change how lines are modified.
    Here, we just convert each line to uppercase.
    """
    return line.upper()

# Ask the user for a filename
filename = input("Enter the filename to read: ")

try:
    # Try to open the input file for reading
    with open(filename, "r") as infile:
        lines = infile.readlines()

    # Create a new file to write the modified content
    new_filename = "modified_" + filename
    with open(new_filename, "w") as outfile:
        for line in lines:
            modified_line = modify_content(line)
            outfile.write(modified_line)

    print(f"File '{filename}' has been read and modified content saved to '{new_filename}'.")

except FileNotFoundError:
    print(f"Error: The file '{filename}' does not exist.")
except PermissionError:
    print(f"Error: You do not have permission to read the file '{filename}'.")
except Exception as e:
    print(f"An unexpected error occurred: {e}")

